<?php

/**
 * Implements hook_theme().
 */
function seminar_theme($existing, $type, $theme, $path) {
    return [
      'my_template' => [
        'variables' => ['test_var' => NULL],
      ],
    ];
  }